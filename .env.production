# Vite reads this at build time for production builds. VITE_-prefixed vars
# become available via import.meta.env in the bundle.
#
# Measurement IDs are not secret — they ship in every gtag call to every
# visitor — so it's fine to commit this file.

VITE_GA_MEASUREMENT_ID=G-CWEHQPGMV8
